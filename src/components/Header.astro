<!-- Necesito crear un header con tailwind -->

<header id="header" class="top-0 fixed w-full">
    <div id="header-content"  class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8 flex justify-between items-center">
        <ul class="flex space-x-4">
            <li>
                <a href="/" class="text-white hover:text-gray-300 font-bold hover:underline transition">Home</a>
            </li>
        </ul>
    </div>
</header>

<style>
    #header{
        animation: scrolling linear both;
        animation-timeline: scroll();
        animation-range: 0 100px;
        transition: 0.5s ease-in-out;
    }

    #header-content {
        display: flex;
        justify-content: flex-start;
        align-items: center;
    }

    @keyframes scrolling {
         to {
            box-shadow: 
                0px 5px 50px -5px hsla(0, 0%, 100%, 0.1)
                0px 0px 0 1px hsla(0, 0%, 100%, 0.1);
            backdrop-filter: blur(10px);
            position: fixed;
        }
    }
</style>